return {
    Debug = true,
    requiredPermission = 'command',
    Command = 'cooldown',    -- Command to Toggle Cooldowns
    GracePeriod = 1,         -- Used for servers that dont allow crime before/after server restarts (Enables ALL cooldowns)
    Webhook = ''
}